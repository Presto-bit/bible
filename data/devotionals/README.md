# 创世记 50 次同行内容包

- 源文件：仓库根目录 `创世记50天默想教材.pdf`、`创世纪灵修书信.pdf`
- 结构化产物：`genesis_50_walk.json`（50 个 session：letter + workbook）
- 配套读经计划：`../plans/genesis_50.csv`（GEN 1–50，一章一天）

## 版权说明

内容标注为「供教会共同体使用 / 2SC 教会共同体」。上线公开分发或商用前，须取得明确书面授权。
App 内展示时会附带 `copyright_note` 字段提示。

## 字段概要

每个 session 含：`day`、`title`、`book`/`chapter`、`focus_verses`、`letter`（body/prayer）、`workbook`（重点、古代处境、脉络、问题、实践、祷告）。
