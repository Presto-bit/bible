import '@/styles/share_landings.css';
import '@/styles/reading_review.css';
import '@/styles/verse_fullscreen.css';

export default function ShareLayout({ children }: { children: React.ReactNode }) {
  return children;
}
